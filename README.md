Ниже README.md, адаптированный под последнюю версию кода (без голосового режима, с OpenAI GPT‑4o‑mini, анализом последней / недели / всех тренировок и интеграцией с Google Sheets)

```markdown
# 🏋️ AI Спортдневник — умный спортивный дневник с AI‑анализом
> 📌 **TL;DR**  
> Telegram-бот, который записывает тренировки в Google Sheets и превращает сухие данные  
> в осмысленный AI-отчёт о нагрузке, восстановлении и прогрессе.

Telegram‑бот для отслеживания тренировок с автоматическим логированием в Google Sheets и анализом тренировок через OpenAI GPT‑4o‑mini.  
Бот считает количество тренировок, время, калории и даёт рекомендации по прогрессу и балансу нагрузки.

## Возможности

- 🤖 **AI‑анализ тренировок (OpenAI)** 
Бот не просто считает минуты и калории, а анализирует паттерны нагрузки,
регулярность, разнообразие и даёт человеческий, тренерский фидбек.

  - анализ одной последней тренировки с кратким разбором и мотивирующим советом;  
  - анализ тренировок за 7 дней;  
  - анализ всех тренировок в Google Sheets.

- 📊 **Google Sheets интеграция**  
  - автоматическая запись каждой тренировки в таблицу;  
  - использование таблицы как основного хранилища истории.

- 📝 **Простой текстовый ввод тренировок**  
  - формат: `Тип, 30 мин, 400 ккал`  
  - пример: `Бег, 30 мин, 400 ккал`.

- 🔔 **Мотивация и контроль регулярности**  
  - напоминание, если не было тренировок более 3 дней;  
  - случайные мотивирующие советы после записи тренировки.

## Структура Google Sheets

Ожидаемая структура таблицы (первая строка — заголовки):

| Колонка | Название   | Пример значения          |
|--------:|------------|--------------------------|
| A       | Дата       | `2026-01-18`       |
| B       | Тип        | `Бег`                    |
| C       | Длительность | `30`                   |
| D       | Калории    | `400`                    |
| E       | Режим      | `FACT` или пусто         |
| F       | Замеры     | `15000` или пусто        |

Бот читает все строки ниже заголовка и использует их для анализа.

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/kiboto30/AI-sportdnevnik.git
cd AI-sportdnevnik
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка окружения

Скопируйте пример `.env` и заполните:

```bash
cp .env.example .env
```

В `.env` должны быть (минимально):

```env
TELEGRAM_BOT_TOKEN=ваш_токен_telegram_бота
GOOGLE_SHEET_ID=ваш_google_sheet_id
GOOGLE_CREDENTIALS_JSON_PATH=credentials.json

# OpenAI API (для AI-анализа)
OPENAI_API_KEY=ваш_openai_api_key
```

> `OPENAI_API_KEY` не обязателен, но без него бот будет выдавать только базовую статистику без AI‑анализа.[web:40]
> ⚠️ Если OpenAI недоступен (нет ключа или ошибка API),  
> бот **не падает**, а возвращает корректную статистику без AI-анализа.


### 4. Настройка Google Sheets

1. Создайте [Google Cloud Project](https://console.cloud.google.com/).  
2. Включите **Google Sheets API**.  
3. Создайте **Service Account**.  
4. Скачайте JSON‑ключ сервисного аккаунта как `credentials.json` в корень проекта.  
5. Поделитесь таблицей (Google Sheet) с e‑mail сервисного аккаунта с правом редактирования.

### 5. Настройка Telegram‑бота

1. Создайте бота через [@BotFather](https://t.me/botfather).  
2. Получите токен и добавьте его в `.env` как `TELEGRAM_BOT_TOKEN`.

## Запуск бота

```bash
python fitness_bot_improved.py
```

Бот начинает polling и принимает сообщения в Telegram.

## Использование

### Основной сценарий

1. Напишите боту любое текстовое сообщение в формате:

   ```text
   Бег, 30, 400 ккал
   Коньки, 45 мин, 350 ккал
   ```

2. Бот:
   - распарсит тип, длительность и калории;  
   - запишет тренировку в Google Sheets;  
   - сделает AI‑разбор именно этой тренировки;  
   - добавит случайный мотивирующий совет.

### Команды бота

- `/start` — приветствие и краткая инструкция.  
- `/analysis_last` — анализ последней тренировки (одна запись + рандомный мотивирующий совет).  
- `/analysis_week` — анализ всех тренировок за последние 7 дней.  
- `/analysis_all` — анализ всех тренировок из таблицы (история целиком).

Если OpenAI недоступен (нет ключа или ошибка API), бот всегда вернёт корректную суммарную статистику по тренировкам (количество, минуты, калории) без AI‑текста.

## Пример формата тренировок

```text
Бег, 30 мин, 400 ккал
Силовая, 45 мин, 300 ккал
Плавание, 32 мин, 450 ккал
Коньки, 60 мин, 500 ккал
```



## Для кого этот проект

- для людей, которые тренируются нерегулярно и хотят видеть реальную картину;
- для разработчиков как пример Telegram-бота с OpenAI и Google Sheets;
- для учебных / портфолио проектов с реальным пользователем и данными.


## Будущие улучшения

- [ ] Расширенный парсинг произвольного текста («бег 5 км за 25 минут» и т.п.).  
- [ ] Отслеживание веса и замеров тела с трендами.  
- [ ] Лидерборд и сравнение нескольких пользователей.    
- [ ] Расширенная логика напоминаний

## Лицензия

MIT License — свободно используйте для личных и учебных проектов.

---

**Сделано с ❤️ для тех, кто хочет видеть прогресс и получать честный AI‑фидбек о тренировках.**
``` AI‑фидбек о тренировках.**
```
